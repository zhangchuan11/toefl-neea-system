(function(a) {
    a.fn.autoScroll = function(d) {
        var f = {
            speed: 40,
            rowHeight: 24
        };
        var e = a.extend({}, f, d),
            b = [];

        function c(h, g) {
            h.find("ul").animate({
                marginLeft: "-=1"
            }, 0, function() {
                var i = Math.abs(parseInt(a(this).css("margin-left")));
                if (i >= g) {
                    a(this).find("li").slice(0, 1).appendTo(a(this));
                    a(this).css("margin-left", 0)
                }
            })
        }
        this.each(function(h) {
            var g = e.rowHeight,
                j = e.speed,
                k = a(this);
            b[h] = setInterval(function() {
                if (k.find("ul").width() <= k.width()) {
                    clearInterval(b[h])
                } else {
                    c(k, g)
                }
            }, j);
            k.hover(function() {
                clearInterval(b[h])
            }, function() {
                b[h] = setInterval(function() {
                    if (k.find("ul").width() <= k.width()) {
                        clearInterval(b[h])
                    } else {
                        c(k, g)
                    }
                }, j)
            })
        })
    }
})(jQuery);