(function(a) {
    a.fn.extend({
        ScrollNews: function(b, j) {
            if (b == null) {
                b = {}
            }
            var g = this.eq(0).find("ul:first");
            var h = g.find("li:first").height();
            var i = b.line ? parseInt(b.line, 10) : parseInt(this.height() / h, 10);
            var f = b.speed ? parseInt(b.speed, 10) : 500;
            var e = b.interval ? parseInt(b.interval, 10) : 2000;
            if (i <= 0) {
                i = 1
            }
            var c = 0 - i * h;
            scrollUp = function() {
                g.animate({
                    marginTop: c
                }, f, function() {
                    for (var k = 1; k <= i; k++) {
                        g.find("li:first").appendTo(g)
                    }
                    g.css({
                        marginTop: 0
                    })
                })
            };
            var d = null;
            g.hover(function() {
                if (d != null) {
                    clearInterval(d)
                }
            }, function() {
                d = setInterval("scrollUp()", e)
            }).mouseout()
        }
    })
})(jQuery);